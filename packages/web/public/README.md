# Static assets

Add a real `icon.png` here (192×192 recommended). It's referenced by
`index.html`, the web manifest, and the push notification payload
(`icon: '/icon.png'`). A placeholder is intentionally not committed as binary.
